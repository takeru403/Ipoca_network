1. DBを作って、抽出した後に正規化をかけるようにする。

2. 読み込んだ後にカテゴリを読み込めるようにする。

3. 正規化処理を実行する。(同じカテゴリ同しで正規化する。)


検索した文章をキーサードにして、そこから近しいスライドを出すように変更していく。

このスライドで見つからない時はperprexyのAPIで検索するもの。

出力の部分をテキストにするのかスライドを生成するのか。

テナント選択に関してカテゴリを表示するようにする。
1


リフト値をそのまま見るようにするでもいい？


1の併売ネットワークの前に
0番のクラスタリングも見れるようにする。
クラスタリングをして、クラスタ番号に名前をつける。


mp3等の重いファイルに関してはS3に保存してから呼び出すように変更する。
